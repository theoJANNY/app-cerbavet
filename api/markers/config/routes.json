{
  "routes": [
    {
      "method": "GET",
      "path": "/markers",
      "handler": "markers.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/markers/count",
      "handler": "markers.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/markers/:id",
      "handler": "markers.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/markers",
      "handler": "markers.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/markers/:id",
      "handler": "markers.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/markers/:id",
      "handler": "markers.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
